import React from 'react';

const CycleCounter = ({ CycleCounter }) => {
  //add 1 to CycleCounter whenever the timer hits zero
  //after every 2 counters - ie 1 focus and break session, log 1 cycle
  //update: probably a better way is to log only the focus time
  //in the event a user wants to skip a break, they still get credited for a pomodoro
};
export default CycleCounter;
